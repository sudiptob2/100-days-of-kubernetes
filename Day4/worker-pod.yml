apiVersion: v1
kind: Pod
metadata:
  name: worker-pod
  labels:
    name: worker-app-pod
    app: demo-voting-app
spec:
  containers:
    - name: worker-app-pod
      image: bretfisher/examplevotingapp_worker:java